# Replace the IP address below with your teammate's actual local IP address on the Wi-Fi network
# Make sure to keep the :3000 at the end (or whatever port their backend is running on)
VITE_BACKEND_URL=http://localhost:3000